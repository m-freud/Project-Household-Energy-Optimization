different profiles call for different prediction methods, independent of predictor.

buy_price, sell_price is always given.
max_charge for evs is a result of the KNOWN price profile and the PREDICTED location of the vehicle.
there is no need to moving_average-predict max_charge.
instead, predict availability.