services:
  influx:
    image: influxdb:2
    ports: ["8086:8086"]
    volumes: ["./influx:/var/lib/influxdb2"]

  grafana:
    image: grafana/grafana
    ports: ["3000:3000"]
    volumes: [
      "./grafana:/var/lib/grafana",
      "./db:/data"
    ]